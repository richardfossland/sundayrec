/** TanStack Query key for the persisted `Settings` (shared by `SettingsPage`
 *  and the App-level i18n hydration so both read/write the same cache entry). */
export const SETTINGS_QUERY_KEY = ["settings"] as const;
