/** TanStack Query keys for the cloud-backup panel (shared by the panel and any
 *  caller that needs to invalidate them). */
export const CLOUD_CONNECTION_KEY = ["cloud", "connection"] as const;
export const CLOUD_QUEUE_KEY = ["cloud", "queue"] as const;
