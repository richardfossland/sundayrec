/** TanStack Query keys for the transcription panel (shared by the panel and any
 *  caller that needs to invalidate them). */
export const WHISPER_MODELS_KEY = ["whisper", "models"] as const;
export const WHISPER_MODEL_STATUS_KEY = ["whisper", "modelStatus"] as const;
