# Build output & deps
dist
node_modules
src-tauri/target
src-tauri/gen
src-tauri/binaries
target

# The ported old Electron renderer is a VERBATIM copy — reformatting it would
# break the byte-for-byte parity with the old app. Leave it exactly as shipped.
legacy

# Kept React reference (ts-rs bindings + hooks) — not part of the build; the
# bindings are generated, so don't reformat them either.
reference

# ts-rs regenerates these on `cargo test` (export_to points here). The frontend
# no longer imports them (the legacy renderer has its own types); they're a
# transient build artifact — see reference/bindings for the committed snapshot.
src/lib/bindings
