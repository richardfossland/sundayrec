# Logs
logs
*.log

# ts-rs regenerates these on `cargo test` (export_to points at the old React
# tree). The legacy renderer doesn't import them; the committed snapshot lives
# in reference/bindings. Transient — don't track.
/src/lib/bindings/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build output
target
src-tauri/target
src-tauri/gen/schemas

# ffmpeg sidecar binaries (fetched per-platform, never committed)
src-tauri/binaries

# Test artifacts
coverage
.last-run.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/
